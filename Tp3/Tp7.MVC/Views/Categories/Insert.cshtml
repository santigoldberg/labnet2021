@model Tp7.MVC.Models.CategoriesView

@{
    ViewBag.Title = "Categories Page";
}

<h1>Insertar Categoria</h1>

@using (Html.BeginForm("Insert", "Categories", FormMethod.Post))
{
    <label>Nombre: </label> @Html.TextBoxFor(c => c.Name)
    <label>Descripcion: </label> @Html.TextBoxFor(c => c.Description)
    <input type="submit" class="btn btn-success" value="Guardar" />
}